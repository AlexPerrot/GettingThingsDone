<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:GettingThingsDone.src.view"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" x:Class="GettingThingsDone.MainWindow"
        Title="Getting Things Done" Height="500" Width="1000"
        Background="{DynamicResource {x:Static SystemColors.AppWorkspaceBrushKey}}">
    <Window.Resources>
        <DataTemplate x:Key="ListTemplate">
            <Border BorderBrush="Black" BorderThickness="3" Margin="5" CornerRadius="2">
                <local:StaticListPanel Width="100"/>
            </Border>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Label Name="ReviewLink" Content="Review" Foreground="AntiqueWhite" Background="Indigo" Grid.Row="0" MouseLeftButtonDown="ReviewButtonClick"
               VerticalAlignment="Center" HorizontalAlignment="Center"
               VerticalContentAlignment="Center" HorizontalContentAlignment="Center"
               Width="200" Height="80" FontSize="18"
               MouseEnter="ReviewLinkEnter" MouseLeave="ReviewLinkLeave" Margin="154,184,142,184" Grid.Column="1"/>
        <Label Name="ProjectsLink" Content="Projects" Foreground="AntiqueWhite" Background="Indigo" Grid.Row="0" MouseLeftButtonDown="ProjectsButtonClick"
               VerticalAlignment="Center" HorizontalAlignment="Left"
               VerticalContentAlignment="Center" HorizontalContentAlignment="Center"
               Width="200" Height="80" FontSize="18"
               MouseEnter="ProjectsLinkEnter" MouseLeave="ProjectsLinkLeave" Margin="152,184,0,184"/>
        <ScrollViewer x:Name="ReviewPanel" Visibility="Collapsed"  VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Grid.Row="0" Grid.ColumnSpan="2">
            <local:ReviewPage/>
        </ScrollViewer>
        <ScrollViewer x:Name="ProjectsPanel" Visibility="Collapsed"  VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Grid.Row="0" Grid.ColumnSpan="2">
            <local:ProjectsPanel/>
        </ScrollViewer>
        <Button Content="Créer une tâche" Grid.Row="1" Click="CreateTask" Grid.ColumnSpan="2"/>
    </Grid>
</Window>
